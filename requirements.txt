# honeypot-soc-lab analysis dependencies
# Core analysis scripts (parse_logs.py, credential_analysis.py, etc.) use
# the Python standard library only.

# Optional: install these for extended features
requests>=2.28.0        # alternative to urllib for geo_analysis.py
python-dateutil>=2.8.2  # enhanced timestamp parsing in parse_logs.py

# Portfolio visualization & IOC export scripts (analysis/)
httpx>=0.27.0           # HTTP client for map_generator.py, ioc_export.py
folium>=0.15.0          # interactive attacker map (map_generator.py)
matplotlib>=3.8.0       # attack timeline charts (timeline_graph.py)
